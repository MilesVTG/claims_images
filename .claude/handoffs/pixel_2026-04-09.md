# Pixel Handoff — 2026-04-09

## What I Built

### Task #3 (CI-072): Photo Upload UI + Standalone Upload Page
Full upload flow from dashboard to GCS pipeline.

**Files created:**
- `dashboard/src/components/shared/PhotoUpload.jsx` — Reusable upload component with file picker, contract/claim ID inputs, FormData upload, status polling (5s interval), terminal spinner, error handling, reset
- `dashboard/src/pages/UploadPage.jsx` — Standalone upload page with manual ID entry

**Files modified:**
- `dashboard/src/api/client.js` — Added `upload()` method (FormData-based, no Content-Type header so browser sets multipart boundary, same auth/401 logic)
- `dashboard/src/pages/ClaimDetailPage.jsx` — Integrated PhotoUpload below PhotoGallery, pre-filled IDs from useParams(), re-fetches claim on complete
- `dashboard/src/App.jsx` — Added `/upload` route
- `dashboard/src/components/layout/Sidebar.jsx` — Added "upload" nav link
- `dashboard/src/styles/theme.css` — PhotoUpload + UploadPage styles

### Task #7: Dashboard Upload Unit Tests
Set up test infrastructure from scratch + wrote tests for all upload components.

**Files created:**
- `dashboard/src/test/setup.js` — jest-dom setup
- `dashboard/src/api/client.test.js` — 7 tests for upload() method
- `dashboard/src/components/shared/PhotoUpload.test.jsx` — 16 tests (file selection, validation, upload flow, polling, errors, reset)
- `dashboard/src/pages/UploadPage.test.jsx` — 3 tests (rendering, no pre-filled IDs)

**Files modified:**
- `dashboard/vite.config.js` — Added vitest config (jsdom, globals, setupFiles)
- `dashboard/package.json` — Added test/test:watch scripts + devDependencies (vitest, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jsdom)

### Task #11 (CI-078): Errors Tab on Health Page
New ERRORS tab showing paginated error log from `GET /api/errors`.

**Files modified:**
- `dashboard/src/pages/HealthPage.jsx` — Added ERRORS to TABS array, ErrorsTab component (scrollable log, refresh, pagination), ErrorEntry component (expandable rows with message/traceback/request_id), error state + fetch logic
- `dashboard/src/styles/theme.css` — ~130 lines of errors CSS (log container, entry rows, badges, traceback, pagination)
- `dashboard/src/components/shared/PhotoUpload.test.jsx` — Fixed header assertion (linter changed `> upload_photo` to `// UPLOAD PHOTO`)
- `dashboard/src/pages/UploadPage.test.jsx` — Fixed heading assertion (linter changed `> upload` to `Upload`)

## API Endpoints Consumed
- `POST /api/photos/upload` — multipart form (file, contract_id, claim_id)
- `GET /api/photos/status/{storage_key}` — polling for pipeline completion
- `GET /api/errors?page=N&per_page=50` — paginated error logs

## Gotchas / Known Issues
1. **Linter modified my initial code** — PhotoUpload header changed from `> upload_photo` to `// UPLOAD PHOTO`, UploadPage heading from `> upload` to `Upload`, and UploadPage wrapped content in a `detail-card`. Tests were updated to match.
2. **No tests for ErrorsTab yet** — only the upload components have tests. ErrorsTab tests should be added in a future session.
3. **`GET /api/errors/stats` not used yet** — the endpoint exists but the Errors tab is raw log view only. Could add summary stats (counts by type/stage/service) above the log later.
4. **Pagination is simple** — prev/next only, no jump-to-page. Fine for now per task spec ("sorting/filtering comes later").

## Final State
- `vite build`: clean (53 modules, 0 errors)
- `vitest run`: 26 tests, all passing
- All 3 tasks marked completed
